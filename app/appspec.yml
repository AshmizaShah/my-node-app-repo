version: 0.0
os: linux

files:
  - source: ./
    destination: /opt/apps

hooks:
  ApplicationStart:
    - location: /opt/apps/scripts/start.sh
      runas: root
      timeout: 300

  ApplicationStop:
    - location: /opt/apps/scripts/stop.sh
      runas: root
      timeout: 300
